# Supabase Seed

Seed files provide local/demo data only. Production tenant provisioning belongs in Batch 22 Identity Platform.

Current seed:

- `001_demo_foundation.sql`: creates one demo tenant and three framework metadata records.
