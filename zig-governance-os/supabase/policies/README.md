# Supabase Policies

Policy source files document reusable RLS patterns. Deployed policies live in migrations.

Current policy source:

- `tenant-isolation.sql`: canonical tenant isolation pattern for tenant-scoped tables.
