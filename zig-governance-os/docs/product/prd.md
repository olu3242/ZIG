# PRD — Zig

## 1. Summary

Zig is an AI-native governance operating system. It replaces the spreadsheet-and-template
approach to GRC with a connected data model (Organization → Project → Asset → Risk →
Control → Framework Requirement → Evidence → Task → Report), an AI Command Center that
generates governance programs directly into that model, an explainable governance score,
and a continuously running Health Advisor. Full context: `docs/vision/product-vision.md`,
`docs/vision/positioning.md`, `docs/product/personas.md`.

## 2. Goals

- Let a new user go from signup to a generated, reviewed, evidence-backed, scored, and
  reported governance program in under 20 minutes (see `docs/product/user-journeys.md`).
- Make governance maturity legible at all times via an explainable score, not a once-a-year
  audit guess.
- Support six frameworks (ISO 27001, SOC 2, NIST CSF, CIS Controls, HIPAA, PCI DSS) as
  metadata on one shared model rather than six separate modules.
- Never present an empty screen.

## 3. Non-goals (this release)

See the exclusions list in `docs/product/mvp-definition.md` — custom frameworks, external
evidence integrations, white-labeling, granular permission overrides, public API, native
mobile.

## 4. Users

Five personas, fully detailed in `docs/product/personas.md`: GRC Manager, Risk/Compliance
Analyst, Consultant, Founder/Small Business Owner, Auditor.

## 5. Product surface — the 11 modules

| # | Module | Core job |
|---|---|---|
| 1 | Mission Control | Single-screen view of governance score, top Health Advisor recommendations, and recent activity for the active project |
| 2 | Guided Project Builder | Intake flow that creates a project and triggers the AI Program Generator |
| 3 | Scenario Workspace | Create/pause/resume/fork/clone alternate governance plans; scenario progress feeds the governance score |
| 4 | Asset Workspace | Inventory of systems, data stores, vendors, and other governed assets |
| 5 | Risk Workspace | Risk register: threat, impact, likelihood, treatment, linked assets |
| 6 | Control Workspace | Control library: implementation status, ownership, framework mappings, linked risks |
| 7 | Evidence Workspace | Evidence artifacts attached to controls, with completeness tracking |
| 8 | Task Workspace | Action items generated by gaps, assigned to owners, tracked to completion |
| 9 | AI Command Center | Program/risk/control generation, framework mapping, gap analysis, readiness assessment, report generation, scenario creation — all explainable |
| 10 | Health Advisor | Continuous gap detection across assets/risks/controls/evidence/owners/reviews/frameworks |
| 11 | Executive Reporting | Portfolio artifacts: asset inventory, risk register, control library, gap assessment, readiness report, governance summary |

No additional modules without a documented justification here first (see `CLAUDE.md`).

## 6. Core requirements by module

### 6.1 Mission Control
- Must display the current governance score with an explanation panel (why, what affects
  it, how to improve it — never a bare number).
- Must surface the top N Health Advisor recommendations, ranked by severity.
- Must never render with a blank state — a brand-new project shows guided next actions
  instead of an empty dashboard.

### 6.2 Guided Project Builder
- Intake must capture: industry, organization size, target framework(s), existing maturity
  if any.
- On submission, triggers the AI Program Generator and lands the user directly in a
  populated Asset Workspace, not a confirmation screen.

### 6.3 Scenario Workspace
- Supports create, pause, resume, fork, and clone.
- A forked/cloned scenario carries over its generated assets, risks, controls, evidence,
  and tasks rather than starting blank.
- Scenario state changes must be reflected in the governance score.

### 6.4 Asset / Risk / Control / Evidence / Task Workspaces
- Each record must carry an organization_id and project_id (tenant isolation) and be
  linkable along the Universal Governance Model chain.
- Each record must show its framework mappings inline, not in a separate framework module.
- Each workspace must support both AI-generated and manually created records side by side.
- List views default to populated demo or AI-generated content — never an empty table with
  only a "create new" button.

### 6.5 AI Command Center
- Every generated record (program, risk, control, mapping, report, scenario) must carry: a
  reason, the supporting data used, a confidence level, and a framework reference where
  applicable.
- Must support: program generation, risk generation, control generation, framework
  mapping, gap analysis, readiness assessment, report generation, scenario creation.

### 6.6 Health Advisor
- Runs continuously (not only on manual trigger).
- Detects: missing assets, missing risks, missing controls, missing evidence, missing
  owners, missing reviews, framework gaps.
- Every recommendation: severity, explanation, action, one-click remediation where
  feasible.

### 6.7 Executive Reporting
- Generates: Asset Inventory, Risk Register, Control Library, Gap Assessment, Readiness
  Report, Governance Summary.
- All artifacts downloadable and generated from live data, not a manually compiled
  document.

## 7. Cross-cutting requirements

- **Multi-tenancy**: every record scoped to Organization → Project; isolation enforced at
  the data layer. Roles: Organization Admin, GRC Manager, Risk Analyst, Compliance
  Analyst, Auditor, Consultant, Viewer. Consultants can access multiple organizations.
- **Framework awareness**: frameworks are metadata attached to assets, risks, controls,
  evidence, tasks, and reports — never a hardcoded per-framework code path.
- **Explainability**: required for both AI recommendations and the governance score.
- **Zero empty states**: every screen, in every module, in every state (new project, new
  org, no data yet) must show demo data, an AI entry point, or a clear next action.
- **Demo data**: three full demo environments — SaaS Startup, Fintech Startup, Healthcare
  Organization — each populated end to end (assets through reports).

## 8. Release plan

Built across five ordered phases ("Fables") — see `docs/implementation/roadmap.md` for the
full breakdown and `CLAUDE.md` for the phase-by-phase build rules.

## 9. Open questions / gaps to resolve before Fable 1 implementation begins

- Exact RBAC permission matrix per role, per module (currently roles are named in
  `docs/architecture/multi-tenant-architecture.md` but the matrix itself is still a stub).
- Exact governance scoring formula and weights (stub in
  `docs/architecture/governance-scoring-engine.md`).
- Final database schema and entity field list (stub in `docs/data/`).
- Full screen inventory and IA (stub in `docs/product/information-architecture.md`).

These four stubs are the highest-priority docs to complete before any Fable 1 application
code is written, per the Documentation-OS rule in `CLAUDE.md`.

## 10. Platform gap analysis

The current enterprise-platform gap analysis lives in `docs/product/gap-analysis.md`.

## 11. Documented module-surface gap: Third-Party / Vendor Risk

Per the "Do not add additional modules unless a clear gap is documented and justified in
`docs/product/prd.md` first" rule in `CLAUDE.md`, this section is that justification,
written before any vendor schema/service/route work begins (`docs/certification/
E2E_GAP_REPORT.md` #7 Vendor — FAIL, `WORKFLOW_TRACEABILITY_MATRIX.md` #10 Vendor
Assessment — OPEN, "no schema, no service, no route").

**Decision: Third-Party/Vendor Risk is NOT a 12th top-level module.** It is justified and
scoped as an **extension of the existing Risk Workspace and Evidence Workspace**, using the
exact same shape Risk Workspace already uses for its own sub-entities
(`risk_assessments`, `risk_acceptances`, `risk_reviews` all hang off `risks` without being
separate modules). Concretely:

- A vendor is a governance object that gets **assessed** (producing a risk posture) and
  **reviewed periodically** (producing evidence) — i.e. it is a specialization of the
  existing Asset → Risk → Evidence chain in the Universal Governance Model, not a new chain.
  `lab_artifacts.artifact_type` already anticipates this with its `'vendor_review'` value
  (added in the Lab Workflow closure, Phase 3).
- New tables (`vendors`, `vendor_assessments`, `vendor_findings`) are scoped under the
  **Risk Workspace** product surface area (module #5), the same way `risk_acceptances` and
  `risk_reviews` are — they do not introduce a new top-level navigation item, a new module
  doc under `docs/modules/`, or a new top-level service key beyond what `RiskService` (or a
  narrowly-scoped sibling reusing its repositories) already owns.
- This does not contradict the 11-module list: the list constrains **top-level product
  surface**, not every sub-entity a workspace manages. Risk Workspace already manages four
  distinct entity types under one module; Vendor Risk becomes a fifth.

This decision applies only to Vendor. It does not retroactively resolve the separate,
still-open Learning/Labs/Career/Academy-vs-11-modules conflict flagged in
`docs/academy/HARMONIZATION_REPORT.md` Section 4, which remains a blocking, undecided
question for those workflows.

It identifies ten foundational capabilities that must be represented in future planning:

- Workflow OS
- Policy Management
- Document Management
- Vendor Risk
- Incident Management
- Issue Management
- Integration Hub
- API Platform
- Observability Platform
- Billing Platform

These are not approved for immediate unordered implementation. They are documented as
platform gaps that require architecture, data, security, acceptance, and module
documentation before coding. Some will remain platform services; some may become
first-class product modules after this PRD is expanded with clear justification.

## 12. Documented module-surface gap: Career Readiness

Per the same `CLAUDE.md` rule invoked in Section 11, this is the justification for Career
Readiness, written before any career-engine code changes (`docs/certification/
E2E_GAP_REPORT.md` #9 Career — FAIL: `CareerReadinessEngine` scores hardcoded literals, no
DB I/O; `WORKFLOW_TRACEABILITY_MATRIX.md` #12 Career Progression — OPEN).

**Decision: Career Readiness is NOT a 12th/13th top-level module.** It is scoped as a
**real-data view under Executive Reporting** (module #11) — it reports a learner's
governance-skill readiness computed from real platform signals, the same way Executive
Reporting already "generates ... Readiness Report, Governance Summary" (Section 6.7) from
live data rather than manually compiled documents. Concretely:

- `apps/web/app/career/page.tsx` is rewritten to compute its readiness score from real,
  persisted `student_twins` columns (`learningScore`, `knowledgeScore`, `skillsScore`,
  already written by the Learning/Assessment/Lab workflow closures) rather than the
  hardcoded `{portfolio: 72, projects: 76, labs: 81, ...}` literals the audit flagged.
  `portfolioScore`/`certificationScore` remain `0`/unset until a future Portfolio Engine
  (already scoped, not built, in `docs/academy/PORTFOLIO_ENGINE_ARCHITECTURE.md`) writes
  them — this is an honest partial rollup, not a fabricated complete one.
- No new top-level navigation item, module doc, or top-level service key is introduced.
  The existing `LearningService`/`AssessmentService`/`ScenarioService` read paths
  (already real, from Phases 1–3) are reused as-is; no `CareerService` is created.
- `behaviorScore`/`confidenceScore` are excluded from the rollup, consistent with
  `docs/academy/CAREER_PATH_CROSSWALK.md`'s finding that no plausible data source for
  either exists anywhere in the codebase — inventing one was explicitly rejected there and
  is not done here either.

This decision applies only to Career Readiness's existing `/career` route. It does not
resolve the separate, still-open Learning/Labs/Academy-vs-11-modules conflict in
`docs/academy/HARMONIZATION_REPORT.md` Section 4, which remains undecided.
